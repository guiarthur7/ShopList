// Ajouter un produit à ma liste
exports.addProduitToListe = async (req, res) => {
  // Ajouter une ligne dans la table liste
  // avec id_user (depuis le token) et id_produit (depuis req.body)
};

exports.deleteProduitToListe = async (req, res) => {};
// Voir tous les produits de ma liste
exports.getMaListe = async (req, res) => {
  // Récupérer toutes les entrées de la table liste
  // où id_user = utilisateur connecté
  // Avec les infos des produits (
};
